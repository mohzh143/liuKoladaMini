% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{kld_plot}
\alias{kld_plot}
\title{Create a quick line plot from kld_values() output}
\usage{
kld_plot(df)
}
\arguments{
\item{df}{A \code{tibble} data frame returned by the \code{kld_values()} function.
It is expected to contain at least the "year" and "value" columns.}
}
\value{
A \code{ggplot} object that can be further customized.
}
\description{
This function generates a simple line and point plot for time-series data
retrieved using the \code{kld_values()} function. It provides a convenient
way to visualize the data without manually setting up a \code{ggplot} object.
}
\examples{
# First, retrieve some data using kld_values()
# This example uses a mock directory to ensure reproducibility.
# In a real scenario, this would call the live API.
\dontrun{
  # Assumes a mock fixture exists for this API call.
  df <- kld_values("0580", "N02282", 2019:2024)

  # Create the plot
  kld_plot(df)
}
}
